{
  "name": "tokenmaxxing-client",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "report": "node reporter/report.js",
    "install-service": "node reporter/install.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "dotenv": "^16.4.0"
  }
}
